#include <stdio.h>
#include <conio.h>

int main()
{
          char cArr[50] = {'\0'};
          
          puts("\n Enter A String => ");
          gets(cArr);
          
          puts("\n Given String Is => ");
          puts(cArr);
          
          getch();
          return 0;
}